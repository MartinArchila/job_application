import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1a73e8', // Adjust to Simplify's primary color
    },
    secondary: {
      main: '#ff7043', // Adjust as needed
    },
  },
});

export default theme;
